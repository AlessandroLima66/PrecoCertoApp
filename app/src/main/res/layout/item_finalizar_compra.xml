<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/item_finalizar_compra_linha_1"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="001 CodigoDeBarras Descricao Quantidade"
        android:textAlignment="textStart"
        android:textColor="@android:color/black"
        android:textSize="14sp"
        android:maxLines="1"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0" />

    <TextView
        android:id="@+id/item_finalizar_compra_linha_2"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="un X ValorUnitario = Valor Total"
        android:textAlignment="textEnd"
        android:textColor="@android:color/black"
        android:textSize="14sp"
        android:maxLines="1"
        app:layout_constraintEnd_toEndOf="@+id/item_finalizar_compra_linha_1"
        app:layout_constraintStart_toStartOf="@+id/item_finalizar_compra_linha_1"
        app:layout_constraintTop_toBottomOf="@+id/item_finalizar_compra_linha_1" />

</android.support.constraint.ConstraintLayout>